;
; Asterisk main config file
;
; Declares the standard FHS paths explicitly - these already match
; Asterisk's own built-in defaults on this system, so this file only
; silences the "Unable to open specified master config file, using
; built-in defaults" startup message; it doesn't change any behavior.
;
[directories](!)
astetcdir => /etc/asterisk
astmoddir => /usr/lib/asterisk/modules
astvarlibdir => /var/lib/asterisk
astdbdir => /var/lib/asterisk
astkeydir => /var/lib/asterisk
astdatadir => /usr/share/asterisk
astagidir => /var/lib/asterisk/agi-bin
astspooldir => /var/spool/asterisk
astrundir => /var/run/asterisk
astlogdir => /var/log/asterisk

[directories]
